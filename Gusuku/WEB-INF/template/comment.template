${report.title}

プロジェクト: ${report.project.name}
KEY         : ${report.key}-${report.seq}
タイプ      : ${report.type.name}
優先度      : ${report.priority.name}

投稿者      : ${writer}

${comment}

http://${host}/Gusuku/report/${report.key}/${report.seq}
